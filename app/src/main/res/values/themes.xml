<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.BarcodeGrader" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/purple_700</item>
    </style>

    <style name="FirebaseSignInTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Use the same colors as in Compose -->
        <item name="colorPrimary">@color/purple_40</item>
        <item name="colorPrimaryDark">@color/purple_grey_40</item>
        <item name="colorAccent">@color/pink_40</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Customize the Toolbar to display the app name -->
        <item name="android:toolbarStyle">@style/CustomToolbarStyle</item>
    </style>

    <style name="CustomToolbarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleTextAppearance">@style/ToolbarTitleText</item>
    </style>

    <style name="ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/purple_40</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>